package com.baseball.hint;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class BallTest {
    List<Integer> inputList = new ArrayList<>(Arrays.asList(1, 2, 3));

//    @DisplayName("플레이어의 입력에서 '볼'카운트하는 테스트")
//    @Test
//    public void 플레이어입력_정답과비교() {
//        List<Integer> answer = new ArrayList<>(Arrays.asList(1, 2, 3));
//        Ball ball = new Ball(inputList, answer);
//        int ballCount = 0;
//        Assertions.assertEquals(ballCount, ball.getBallCount());
//    }
}
